# frozen_string_literal: true

module ShoppingCart
  module OrderConst
    IN_PROGRESS = :in_progress
    IN_QUEUE = :in_queue
    IN_DELIVEY = :in_delivery
    DELIVERED = :delivered
    CANCELED = :canceled
  end
end
