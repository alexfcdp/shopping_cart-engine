tr
  td
    .general-img-wrap-table
      = link_to(image_tag(main_app.url_for(product.cover[:url]), alt: product.cover[:name], class: "cart-img-shadow pull-left"), main_app.book_path(product))
  td
    p.title
      = link_to product.title, main_app.book_path(product)
  td
    span.font-16.in-gold-500
      = "#{t('currency_sign')}#{order_item.price}"
  = form_for order_item, remote: true do |f|
    td
      .input-group
        = link_to "#{order_item_path(id: order_item.id, quantity: order_item.quantity - 1)}", method: :put, remote: true, class: "input-link" do
          i.fa.fa-minus[style="line-height: 35px;"]
        = f.text_field :quantity, value: order_item.quantity.to_i, disabled: "disabled", class: "form-control quantity-input", min: 1
        = f.hidden_field :product_id, value: product.id
        = link_to "#{order_item_path(id: order_item.id, quantity: order_item.quantity + 1)}", method: :put, remote: true, class: "input-link" do
          i.fa.fa-plus[style="line-height: 35px;"]
  td
    span.font-16.in-gold-500
      = "#{t('currency_sign')}#{order_item.sub_total}"
  td
    = link_to order_item, {data: {confirm: "#{t('cart.confirm')} '#{order_item.product.title}' #{t('cart.confirm_end')}"}, method: :delete, remote: true, class: "close general-cart-close"} do
      span[aria-hidden="true"]
        | ×